# コントリビューションについて

## テンプレートの Pull Request

歓迎します。ただし先に言っておきます——**入るかどうかは分かりません。**

同梱テンプレートはメンテナが目を通したものとして配布されるので、**入れる以上はこちらの責任**になります。
そのため内容を見て判断します。落としても他意はありません。

**入れられないもの:**

- ルールブックの**本文をそのまま転記した**もの
- 出どころが分からないもの

**入らなかった場合でも、あなたのテンプレートはあなたが自由に配布できます。**
テンプレートを JSON ファイル1つで完結する形にしてあるのは、そのためでもあります
（このリポジトリが唯一の配布経路にならないように、という設計です）。

## コードの Pull Request

同じく歓迎しますが、**先に Issue で相談してもらえると助かります**。
このプロジェクトは設計の意図が細かく決まっている部分があり、
実装だけ見ると「なぜこうなっているのか」が分からない箇所があります。

## バグ報告

再現手順があると助かります。**動かなかった環境（ブラウザとバージョン）**を添えてください。
File System Access API など、ブラウザによって挙動が違う機能を使っています。
